import { validate_guess, check_win_or_lose } from "../src/game.js";

describe("Test validate_guess function", () => {
  test("PLACEHOLDER", () => {
    // Arrange
    const num = 9;

    // Act
    const result = 5;

    // Assert
    expect(result).not.toEqual(num);
  });
});

describe("Test check_win_or_lose function", () => {
  test("PLACEHOLDER", () => {
    // Arrange
    const num = 9;

    // Act
    const result = 5;

    // Assert
    expect(result).not.toEqual(num);
  });
});
